<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/black"
    android:padding="24dp">

    <!-- Judul: KINETIC dengan Efek Glow -->
    <TextView
        android:id="@+id/tv_welcome_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="KINETIC"
        android:textColor="@color/neon_green"
        android:textSize="40sp"
        android:textStyle="bold|italic"
        android:fontFamily="@font/lexend_regular"
        android:letterSpacing="0.2"
        android:shadowColor="#CCFF00"
        android:shadowDx="0"
        android:shadowDy="0"
        android:shadowRadius="15"

        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/btnSignup"
        app:layout_constraintVertical_chainStyle="packed" />

    <!-- Deskripsi Custom: Catat Larimu -->
    <TextView
        android:id="@+id/tv_welcome_desc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Catat larimu, pantau progresmu, dan lampaui batas dirimu setiap hari."
        android:textColor="@color/white"
        android:textSize="14sp"
        android:letterSpacing="0.15"
        android:lineSpacingExtra="4dp"
        android:fontFamily="@font/lexend_regular"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/tv_welcome_title" />

    <!-- Tombol GET STARTED (Warna Neon) -->
    <Button
        android:id="@+id/btnSignup"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_marginBottom="8dp"
        android:backgroundTint="@color/neon_green"
        android:text="GET STARTED"
        android:textColor="@color/black"
        android:textSize="16sp"
        android:textStyle="bold"
        android:fontFamily="@font/lexend_regular"
        app:cornerRadius="12dp"
        app:layout_constraintBottom_toTopOf="@+id/layout_signin_container" />

    <!-- Container untuk Teks & Tombol Sign In Neon -->
    <LinearLayout
        android:id="@+id/layout_signin_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="40dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ALREADY A MEMBER?"
            android:textColor="#B7B5B4"
            android:textSize="14sp"
            android:letterSpacing="0.1"
            android:fontFamily="@font/lexend_regular" />

        <Button
            android:id="@+id/btnLogin"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="LOG IN"
            android:textColor="@color/neon_green"
            android:textSize="14sp"
            android:textStyle="bold"
            android:fontFamily="@font/lexend_regular"
            android:paddingStart="2dp"
            android:paddingEnd="8dp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>